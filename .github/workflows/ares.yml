name: Build Kernel CI ARES

on:
  workflow_dispatch:
  push:
jobs:
  build:
    name: Build Xiaomi ARES Kernel
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          git ccache automake flex lzop bison gperf build-essential zip curl zlib1g-dev \
          g++-multilib libxml2-utils bzip2 libbz2-dev libbz2-1.0 libghc-bzlib-dev \
          squashfs-tools pngcrush schedtool dpkg-dev liblz4-tool make optipng maven \
          libssl-dev pwgen libswitch-perl policycoreutils minicom libxml-sax-base-perl \
          libxml-simple-perl bc libc6-dev-i386 libx11-dev lib32z-dev \
          libgl1-mesa-dev xsltproc unzip device-tree-compiler

    - name: Download Toolchains
      run: |
        # Create a directory for all toolchains
        mkdir -p $GITHUB_WORKSPACE/toolchains
        cd $GITHUB_WORKSPACE/toolchains

        # Clone Proton Clang (replaces AOSP Clang and GCC)
        echo "Downloading Proton Clang..."
        git clone https://github.com/kdrag0n/proton-clang.git --depth=1

    - name: Clone kernel source
      run: |
        git clone --branch lxr --depth 1 https://github.com/Tkpointz/android_kernel_aresin.git

    - name: Build Kernel
      run: |
        # Set environment variables for the build
        export KBUILD_BUILD_USER="GitHub"
        export KBUILD_BUILD_HOST="Actions"
        export ARCH=arm64
        export SUBARCH=arm64
        export PATH="$GITHUB_WORKSPACE/toolchains/proton-clang/bin:$PATH"
        
        # Setup ccache (optional but recommended for faster builds)
        ccache -M 10G
        
        # Change to kernel source directory
        cd android_kernel_aresin
        
        # Clean up previous builds (reference script style)
        make clean && rm -rf out
        mkdir out
        
        # Define common build arguments based on reference script
        export ARGS="-j$(nproc --all) \
                    ARCH=arm64 \
                    SUBARCH=arm64 \
                    O=out \
                    CC=clang \
                    CROSS_COMPILE=aarch64-linux-gnu- \
                    CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
                    CLANG_TRIPLE=aarch64-linux-gnu- \
                    AR=llvm-ar \
                    NM=llvm-nm \
                    OBJCOPY=llvm-objcopy \
                    OBJDUMP=llvm-objdump \
                    STRIP=llvm-strip"
        
        # Run mrproper, defconfig, and prepare (adapted from reference)
        make $ARGS mrproper
        make $ARGS aresin_defconfig
        # Note: menuconfig is interactive; skipping unless needed. If required, add it here.
        make $ARGS prepare
        
        # Final build
        make $ARGS

    - name: Build Custom Kernel Module
      run: |
        # Set the same environment variables as the kernel build
        export KBUILD_BUILD_USER="GitHub"
        export KBUILD_BUILD_HOST="Actions"
        export ARCH=arm64
        export SUBARCH=arm64
        export PATH="$GITHUB_WORKSPACE/toolchains/proton-clang/bin:$PATH"
        
        # Define common build arguments based on reference script
        export ARGS="-j$(nproc --all) \
                    ARCH=arm64 \
                    SUBARCH=arm64 \
                    CC=clang \
                    CROSS_COMPILE=aarch64-linux-gnu- \
                    CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
                    CLANG_TRIPLE=aarch64-linux-gnu- \
                    AR=llvm-ar \
                    NM=llvm-nm \
                    OBJCOPY=llvm-objcopy \
                    OBJDUMP=llvm-objdump \
                    STRIP=llvm-strip"
        
        # Build the external module
        # -C: Points to kernel source output directory (O=out)
        # M=: Points to your module source directory
        make $ARGS -C android_kernel_aresin/out \
             M=$GITHUB_WORKSPACE/kernel \
             modules

    - name: Upload Kernel Image
      uses: actions/upload-artifact@v4
      with:
        name: Kernel-Image-begonia
        path: android_kernel_aresin/out/arch/arm64/boot/Image.gz
    - name: Upload Kernel Module
      uses: actions/upload-artifact@v4
      with:
        name: Kernel-Module-khack
        # Compiled .ko file will be in your module source directory
        path: kernel/khack.ko
